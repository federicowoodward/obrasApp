comando que funciona para crear migracion: 
npx typeorm migration:create src/migrations/InitSchemaAndSeeds -- -d src/config/data-source.ts

await queryRunner.query(`
        INSERT INTO plan_limit (name, max_elements, max_deposits, max_constructions, max_workers)
        VALUES 
        ('Free', 10, 1, 1, 2),
        ('Starter', 50, 3, 2, 10),
        ('Pro', 200, 10, 10, 50),
        ('Enterprise', 1000, 50, 50, 500);
    `);

// 🌱 Seeds: Category
await queryRunner.query(`
        INSERT INTO category (name)
        VALUES 
        ('Material'),
        ('Herramienta'),
        ('Elemento de seguridad');
    `);